<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="houzz.mapper.LoginMapper">
	<select id="loginselect" parameterType="string" resultType="authInfoDTO">
        select member_id userId, member_Pw userPw, member_email userEmail, 'mem' grade
        from member
        where member_id= #{userId}
        union
        select mediation_id userId, mediation_Pw userPw, MEDIATION_EMAIL userEmail, 'med' 
        from mediation
        where mediation_id= #{userId}
        
	</select>

</mapper>